# checkpoints（占位）

此目录仅作与对方工程对齐的**文件夹预留**。

- 本仓库**不在此下载**大模型权重。
- 对方宿主可在此放置 `Qwen*` / `chinese-clip*` 等；本项目侧不改对方选模逻辑。
- 王兴视频真伪 `.pt` 的项目验证路径见：
  `outputs/vedio_pred/models/wangxing_dual_scale_classifier.pt`
- 部署到服务器时再挂载真实权重。
