# 论文与技术研究记录

> 归档性质：非核心技术积累与论文研究，不进入正式工作周报、月报或核心工作产出统计。
>
> 核查日期：`2026-08-13`
>
> 主要论文来源目录：`C:\Users\zhanghaotian\Desktop\工作\论文`

## 一、使用边界

- 本记录用于保存论文阅读、开源项目部署/复现、个人工程实验和历史技术积累。
- `2026-05-01` 之前的内容保留在本记录中，但不因为被记录就自动变成正式工作。
- 2026年5月之后下载或继续研究的论文，如果没有对应的正式任务、交付物和工作记录，也仍按研究归档处理。
- 论文中的指标、用户研究和实验结论属于论文作者的结果；本地项目的结果另行记录，不能混写。
- 本次对论文目录逐文件核查，确认有 `7` 篇 PDF。没有把文件名、目录时间或项目名直接当成复现实验结论。

## 二、论文清单与证据等级

证据等级定义：

- `A：本地有对应代码和运行产物`，可以记录为本地部署/复现或工程实验。
- `B：本地有相关代码、配置或素材`，只能记录为技术参考或部分工程验证。
- `C：目前只确认论文文件或阅读记录`，不能写成本地复现。

| 论文 | 论文时间线索 | 本地对应情况 | 等级 |
| --- | --- | --- | --- |
| Diffusion Renderer: Neural Inverse and Forward Rendering with Video Diffusion Models | arXiv 版本 `2025-03-22`；文件 `2501.18590v2.pdf` | `光影\DiffusionRenderer` 有配置、训练、推理和模型目录，但本次未确认论文级完整复现实验结果 | B |
| InstaFace: Identity-Preserving Facial Editing with Single Image Inference | arXiv 版本 `2025-03-07`；文件名为中文译名 | 有身份保持、面部编辑和重打光方向的相关工程，但未找到 InstaFace 原论文的直接复现目录 | C |
| RenderFormer: Transformer-based Neural Rendering of Triangle Meshes with Global Illumination | SIGGRAPH 2025；文件 `renderformer-paper.pdf` | `D:\Pycharm2023.21\project\renderformer` 有官方代码结构、预训练权重、推理脚本和多组 PNG/EXR 输出 | A |
| Facial Microscopic Structures Synthesis from a Single Unconstrained Image | SIGGRAPH 2025；文件为中文译名 | 找到面部细节、纹理和取证相关工程，但未确认 FLEX 方法的直接实现 | C |
| PI-Light: Physics-Inspired Diffusion for Full-Image Relighting | arXiv 版本 `2026-01-29`；文件 `2601.22135v1.pdf` | 有 `IC-Light`、`DiffusionRenderer`、`LightCtrl` 等相关工程，但不是 PI-Light 原论文复现 | B |
| LightPainter: Interactive Portrait Relighting with Freehand Scribble | CVPR 论文 PDF；文件 `LightPainter.pdf` | 有光照控制和人像重打光工程，但未发现自由手绘 scribble 训练流程的直接复现 | C |
| BodyReLux: Temporally Consistent Full-Body Video Relighting | 论文标注为 `May 2026`；文件名为中文译名 | `LightCtrl` 有视频重打光和轨迹控制代码，但不等同于 BodyReLux 的主体专属训练和采集方案 | B |

## 三、逐篇论文研究记录

### 1. Diffusion Renderer

**论文标题**

`Diffusion Renderer: Neural Inverse and Forward Rendering with Video Diffusion Models`

**文件**

`C:\Users\zhanghaotian\Desktop\工作\论文\2501.18590v2.pdf`

**核心方法**

- 将神经逆渲染和神经前向渲染放在同一个视频扩散框架中。
- 逆渲染阶段从图像或视频估计几何、材质和光照相关的 G-buffer。
- 前向渲染阶段根据 G-buffer 和环境光条件生成照片级图像，不依赖完整的显式路径追踪。
- 使用合成数据训练基础能力，再用逆渲染模型为真实视频自动生成伪标签，减轻真实配对数据缺失的问题。
- 论文使用 Stable Video Diffusion 作为基础模型，并通过专门的属性条件和环境光编码进行控制。

**论文原文结论**

- 论文报告其方法在逆渲染、前向渲染和重新打光任务上超过对比方法，尤其强调复杂阴影、反射和真实场景泛化。
- 单步模型在误差敏感的光度指标上具有速度和稳定性优势，但在高频细节和感知质量上可能更模糊。
- 多步随机模型更适合重新打光和前向渲染中的真实细节生成。

**论文原文局限**

- 基于 Stable Video Diffusion，推理仍偏离线，速度有待通过蒸馏优化。
- 编辑结果可能出现轻微颜色或纹理变化。
- 真实视频自动标注依赖现成光照估计器，标注误差会限制效果。

**本地对应与结论**

- 本机存在 `D:\Pycharm2023.21\project\光影\DiffusionRenderer`，包括 `configs`、`inference`、`train`、`models` 等目录。
- 当前证据足以记录为“部署/工程参考”，不足以记录为“完成论文复现”。
- 对当前工作的启发是：光照控制不只是修改亮度，还需要把几何、材质、环境光和时序一致性作为条件；这属于技术路线总结，不是本地复现实验结果。

### 2. InstaFace

**论文标题**

`InstaFace: Identity-Preserving Facial Editing with Single Image Inference`

**文件**

`C:\Users\zhanghaotian\Desktop\工作\论文\InstaFace：带有单一图像推断的身份保护面部编辑.pdf`

**核心方法**

- 只使用一张推理图像进行身份保持的面部编辑。
- 使用 3DMM 条件图和 3D Fusion Controller，同时控制姿态、表情和光照。
- 使用面部识别编码器保留身份细节，再使用 CLIP 图像编码器保留背景、发型、饰品等更广义语义。
- 先在 FFHQ 和 DECA 条件上学习面部重演属性，再针对单张身份图像进行微调。

**论文原文结论**

- 论文报告其方法在身份保持、照片真实感、姿态/表情/光照控制方面优于若干对比方法。
- 面部识别特征和 CLIP 语义特征结合后，身份和非面部上下文保持更稳定。
- 单图推理降低了多图身份数据的要求，适合身份保持的面部编辑场景。

**论文原文局限**

- 输入为偏角或极端姿态时，身份可能偏离目标。
- 极端亮度条件下可能出现颜色准确性问题。
- DECA 的估计误差可能引入轻微表情偏差。

**本地对应与结论**

- 本机有 `光影\IdentityRelightCommon`、`光影\FaceLight` 等面部光照和身份相关工程，但没有发现名称和流程都对应 InstaFace 的直接复现。
- 当前应归档为“身份保持面部编辑方向的论文参考”，不能写成“使用了 InstaFace 完成身份编辑”。
- 对当前评估器的启发是：身份一致性需要结合脸部局部特征和整体语义，不能只依赖单一人脸相似度。

### 3. RenderFormer

**论文标题**

`RenderFormer: Transformer-based Neural Rendering of Triangle Meshes with Global Illumination`

**文件**

`C:\Users\zhanghaotian\Desktop\工作\论文\renderformer-paper.pdf`

**核心方法**

- 将三角网格场景编码为序列 token，用 Transformer 直接预测带全局光照的图像。
- 第一阶段建模三角形之间与视角无关的光传输。
- 第二阶段将光线束 token 转换为像素块，并结合第一阶段的三角形序列生成图像。
- 通过三维空间位置编码表达三角形相对位置，不需要针对每个场景单独训练。

**论文原文结论**

- 论文显示 RenderFormer 可以在不进行场景级微调的情况下处理反射、阴影、间接光和多光源。
- 训练范围内的三角形数量、光源数量和视角变化可以较好地泛化。
- 多阶段 Transformer 的拆分能够兼顾全局光传输和视角相关成像。

**论文原文局限**

- 注意力计算为二次复杂度，训练和推理的三角形数量受到资源限制。
- 超过训练范围的光源数量、光源尺寸、反射次数、相机位置和分辨率会出现细节丢失。
- 场景内光源、彩色光源和空间变化材质仍需要扩展训练或组合策略。

**本地对应与结论**

- 本地 `D:\Pycharm2023.21\project\renderformer` 有 `infer.py`、`scene_processor`、`renderformer\models`、示例场景和预训练权重。
- `output` 目录中确认存在多组 PNG/EXR 渲染结果，包括 `cbox` 和 `blend_scene` 场景。
- 这篇论文可以记录为“本地开源项目部署/推理验证”，但本地输出不是论文表格的重新测量。
- 论文对当前工作的直接启发是：复杂光照可以被拆成场景条件、光传输和视角成像三个层次；同时要显式记录模型训练分布外的失效边界。

### 4. Facial Microscopic Structures Synthesis

**论文标题**

`Facial Microscopic Structures Synthesis from a Single Unconstrained Image`

**文件**

`C:\Users\zhanghaotian\Desktop\工作\论文\单张无约束图像的面部显微结构综合.pdf`

**核心方法**

- 提出 FLEX，从单张自然拍摄的人脸图像合成皱纹和毛孔等面部微结构。
- 使用参数化的皱纹/毛孔表示和图神经网络模拟相邻微结构的关系。
- 使用 Direction Distribution Similarity 约束皱纹方向分布，同时结合视觉相似度进行优化。
- 生成微结构位移图，并可与 FaceScape、DECA、HRN 等宏观面部重建方法组合。

**论文原文结论**

- 论文报告 FLEX 能从模糊、无约束的面部皮肤图像中恢复具有相似密度和方向特征的微结构。
- 方向分布约束和视觉相似度联合使用时，效果优于只使用单一目标或简单噪声的方法。
- 论文强调微结构可以作为宏观面部几何之外的真实感补充。

**论文原文局限**

- 主要表示凹陷结构，无法覆盖鼻尖等区域的凸起结构。
- 微结构受光照和视角影响，跨光照条件不能保证一致。
- 论文明确提到高保真面部微结构合成存在被用于伪造的风险。

**本地对应与结论**

- 本机存在面部细节、纹理和取证相关代码，但未找到 FLEX 的直接实现、训练集或论文级复现实验。
- 当前评估器中的纹理取证与这篇论文属于相关技术方向，不应表述为“采用 FLEX 完成纹理检测”。
- 可借鉴的工程原则是：面部纹理质量需要区分宏观结构、局部高频细节和光照造成的外观变化。

### 5. PI-Light

**论文标题**

`PI-Light: Physics-Inspired Diffusion for Full-Image Relighting`

**文件**

`C:\Users\zhanghaotian\Desktop\工作\论文\2601.22135v1.pdf`

**核心方法**

- 用两阶段框架处理全图重新打光：先进行图像内在属性估计，再进行神经前向渲染。
- 使用 batch-aware attention 改善一组图像中内在属性预测的一致性。
- 在前向渲染中加入物理启发的光传输约束，让扩散模型更接近可解释的光照变化。
- 使用只表示前半球的光照条件，减少自发光物体和背景已有光照的干扰。
- 构建受控光照的数据集，支持逆渲染和前向渲染训练。

**论文原文结论**

- 论文报告 PI-Light 在高光、漫反射和真实场景泛化方面优于若干对比方法。
- 物理启发损失可以在较少数据和计算量下改善光照控制和泛化。
- 将光照分解为可控制的表示，有助于保持背景一致性并控制光照方向和强度。

**论文原文局限**

- VAE latent 与 RGB 并非严格空间对齐，latent mask 会产生颜色和模糊误差。
- 内在分解具有歧义，严重误差可能传递到最终重新打光结果。
- 当前前半球光照设定不能自由添加或修改后半球可见光源。
- 材质表示未显式覆盖 IOR、sheen、coat 等因素，可能把这些因素吸收到 metallic 等预测中。

**本地对应与结论**

- 本机有 `光影\IC-Light`、`光影\DiffusionRenderer` 和 `光影\LightCtrl` 等相关工程。
- 这些工程可以作为物理约束、光照分解和工程落地的参考，但没有证据表明本地已经复现 PI-Light。
- 对 LTX2.3/LightCtrl 工作的启发是：控制轨迹和光照参数需要单独记录，不能只依赖自然语言 Prompt。

### 6. LightPainter

**论文标题**

`LightPainter: Interactive Portrait Relighting with Freehand Scribble`

**文件**

`C:\Users\zhanghaotian\Desktop\工作\论文\LightPainter.pdf`

**核心方法**

- 通过用户在肖像图像上绘制的自由手 scribble 交互控制重打光效果。
- 使用 scribble simulation 生成不需要人工逐条标注的训练条件。
- 使用局部连接的 shading stripes 表示稀疏、局部和带方向的涂画输入。
- 采用两阶段重打光：先从 scribble 和几何条件补全合理的 shading map，再结合学习到的 albedo 生成最终外观。
- 使用 SkinFill 让用户控制皮肤色调，并缓解训练数据中的肤色偏差。

**论文原文结论**

- 论文报告其系统可以把少量自由手输入转换为局部、可控且较自然的人像光照效果。
- 合成 scribble 与两阶段网络有助于提高对真实用户输入的泛化。
- 用户研究中，论文方法相较若干商业或已有工具获得更高偏好。

**本地对应与结论**

- 本机 `光影\LightCtrl` 有光照控制脚本、轨迹配置、模型和视频输出，但其输入方式是光照轨迹/参数，不是 LightPainter 的自由手 scribble。
- 本地 `LightCtrl` 结果应记为独立工程实验，不能写成 LightPainter 复现。
- 这篇论文对交互设计的启发是：把复杂光照参数转成局部、可视化、低门槛的用户控制信号。

### 7. BodyReLux

**论文标题**

`BodyReLux: Temporally Consistent Full-Body Video Relighting`

**文件**

`C:\Users\zhanghaotian\Desktop\工作\论文\BodyRelux高定制化.pdf`

**核心方法**

- 面向主体专属的全身视频重新打光。
- 使用 OLAT 和动态表演采集构建像素对齐的多光照视频对。
- 使用 OLAToken 将每个光源表示为带强度和方向的 token。
- 用 masked attention 处理动态光照序列，支持多个光源、方向光、点光、渐变光和 HDRI。
- 以视频扩散模型为骨干，并使用空间条件保持输入表演和细节。

**论文原文结论**

- 论文报告其方法在全身、半身和特写视频上都能保持较好的时序一致性。
- 光源 token 和动态光照条件使模型可以处理静态、动态、多光源以及 HDRI 光照控制。
- 论文展示了主体专属训练对高质量视频重打光的重要性。

**论文原文局限**

- 采集灯光主要位于上半球，不能覆盖下半球和近场光源。
- 模型是主体专属的，不是面向任意未知身份的通用模型。
- 多主体严重遮挡和主体之间的投影关系仍然困难。

**本地对应与结论**

- 本机 `光影\LightCtrl` 有视频重打光、轨迹配置和输出结果，现有工作中的左右移动、上下移动、环绕等路线与论文的动态光照控制问题相关。
- 但本地没有 BodyReLux 的 LED Sphere 采集数据、主体专属 WAN2.2 训练链路或论文级评测。
- 因此只能记录为“视频重打光技术路线参考”，不能把 LightCtrl 结果写成 BodyReLux 结果。

## 四、论文对当前技术路线的综合结论

### 1. 光影与重新打光

7 篇论文中，Diffusion Renderer、PI-Light、LightPainter 和 BodyReLux 都指向同一个演进方向：从单纯的图像亮度修改，逐步转向带几何、材质、光源、用户控制和时序一致性的条件生成。当前本地的 `IC-Light`、`DiffusionRenderer`、`FaceLight` 和 `LightCtrl` 分别覆盖了其中一部分，但不是这些论文方法的统一复现。

### 2. 身份和面部细节

InstaFace 关注身份、背景和面部属性控制；FLEX 关注皱纹、毛孔等高频微结构。两者共同说明：面部真实性不能只用整体图像相似度判断，还需要区分身份特征、局部结构、纹理细节和光照变化。这与当前评估器的身份、AU、面部纹理和人工复核方向相关，但属于技术参考关系。

### 3. 神经渲染与失效边界

RenderFormer 说明 Transformer 可以学习三角网格到图像的全局光照映射，但训练分布中的三角形数量、光源数量、相机范围和反射深度会直接决定泛化边界。这个结论可以迁移到当前评估器：评估结果应保留输入条件、模型版本、视频长度和控制参数，不能只保存一个综合分。

### 4. 对实验记录的要求

- 区分论文作者指标、本地复现指标和工程样例观察。
- 对每个重新打光结果记录光源轨迹、方向、强度、颜色、帧数和帧率。
- 对身份或纹理结果区分身份保持、局部细节、光照稳定和时序一致性。
- 对没有跨模型、跨生成批次验证的结果，不写成普适结论。

## 五、开源项目与个人技术积累归档

以下项目属于非核心技术积累或个人工程实验。目录时间只能说明本机文件活动，不等同于正式工作日期。

| 方向 | 本机项目/目录 | 当前核验结果 | 归档结论 |
| --- | --- | --- | --- |
| 视频生成与扩散模型 | `FramePack`、`ComfyUI_windows_portable`、`stable-diffusion-webui` | 有本地项目目录和运行环境 | 通用模型与工具积累，不纳入本次核心工作 |
| 3D 世界和视频生成 | `HY-World-2.0`、`VideoFrom3D`、`DiffusionAsShader` | 有本地项目目录 | 3D/视频生成路线研究，不纳入核心工作 |
| 神经渲染 | `renderformer` | 有代码、权重、推理和 PNG/EXR 结果 | A 级开源项目部署验证，仍属于非核心归档 |
| 光照与重打光 | `光影\DiffusionRenderer`、`光影\IC-Light`、`光影\UniRelight` | 有代码、模型、配置或推理素材 | B 级工程参考/部分验证，不等同论文复现 |
| 人像光照工程 | `光影\FaceLight` | 有训练、推理、评估说明和输出目录 | 个人工程实验；与面部光照论文相关但不等同论文结果 |
| 视频光照控制 | `光影\LightCtrl` | 有多个控制脚本、轨迹配置、模型和视频审查产物 | 若属于 2026年5月之后正式任务，另列核心工作；技术路线本身保留在本档案 |
| 语音和数字人 | `Amphion`、`VoxCPM`、`VibeVoice`、`Qwen3-TTS`、`SentiAvatar`、`so-vits-svc-4.1-Stable` | 有本地项目目录 | 语音/数字人方向积累，不纳入本次评估器核心统计 |
| 大模型、RAG、Agent | `LLaMA-Factory`、`langchain`、`Langchain-Chatchat`、`Dify`、`SimpleMem`、`generative_agents` | 有本地项目目录或工程材料 | 应用型大模型积累，不纳入本次评估器核心统计 |
| 历史内容生成 | 游戏解说、文物、太阳神鸟场景、塔罗等目录 | 有历史项目或素材 | 历史工程归档，不进入 2026年5月至今工作报告 |

## 六、可直接引用的归档表述

### 论文研究

> 阅读并整理 Diffusion Renderer、PI-Light、BodyReLux、LightPainter、RenderFormer、InstaFace 和 Facial Microscopic Structures Synthesis 等论文，重点关注神经渲染、身份保持、面部微结构、光照控制和视频时序一致性。相关内容作为非核心技术积累和后续方案参考，不计入正式工作周报及核心工作产出统计。

### 开源项目和个人技术积累

> 保留 FramePack、ComfyUI、RenderFormer、Diffusion Renderer、IC-Light、UniRelight、FaceLight、LightCtrl、语音/数字人、大模型应用和历史工程等本地项目与实验记录。已区分“有本地运行产物”“有代码或配置”“仅有论文/项目资料”三类证据，避免把开源项目部署或论文阅读直接写成正式交付。

### 与当前工作的关系

> 论文和开源项目主要用于形成技术背景和方案判断；当前核心工作中的评估器、Wang Xing 专项、真人/Seedance 取证、人工复核、表情数据集和光影实验，仍以各自的本地代码、数据、结果文件和时间记录为准。

## 七、后续补充规则

- 新增论文时，至少记录标题、文件路径、核心方法、论文结论、论文局限和本地对应证据。
- 新增开源项目时，至少记录 README、代码目录、运行命令、输出目录和运行状态。
- 只有当项目有明确任务归属、时间范围和可核验产出时，才可以从本归档转入核心工作报告。
- 如果暂时无法确认是否属于正式工作，先放入本归档的“待确认”状态，不直接计入核心统计。
## 2026-08-24 更新：王兴真伪检测研究

本次更新在原有论文阅读记录之后追加，不覆盖历史内容。

### 当前实验结论

- 旧 V3 仍是当前分类指标最好的候选：25+25 准确率 98%，AI 召回率 100%，实拍召回率 96%。
- V4.1、V4.2、V4.3、V4.4 虽然加入 AU 关系、Temporal Transformer、landmark 和 face-crop，但实际测试均未超过 V3。
- V4.x 退步的主要原因不是网络层数不足，而是删除全帧监督后，纯表情特征的信息量不足；同时训练集只有实拍和 Seedance，没有 LoRA/多图排序监督。
- `ppt_video` 两组共 8 个视频只能作为排序开发和验收集，不能替代成规模排序训练集。

### 论文方法的本地落地状态

- AUNet：已落地轻量 AU relation attention，尚未完整复现论文的关系 Transformer 和 AU 区域扰动训练。
- LipForensics：已参考嘴部时序方向，当前仅有 landmark/face-crop 轻量分支，尚未使用论文级嘴部时空 backbone。
- FTCN/Temporal Transformer：已落地轻量 Temporal Conv 和 Transformer，尚未达到论文完整视频特征训练协议。
- Multi-Attentional Deepfake Detection：已按眼睛、嘴部、眉毛、下脸划分局部区域，区域独立监督仍需加强。
- DeepfakeBench：已采用固定采样、分组划分、独立测试和双向召回率口径。

### 后续判断

下一步不再单独训练纯表情模型替换 V3，而是保留 V3 监督真实性分支，加入表情主头和低权重 face-crop 辅助头。最终分数由 validation 学习，但限制分支权重范围，避免身份或 crop 分支成为捷径。

```text
V3 真实性基础分支
+ AU/Blendshape/landmark 表情主分支
+ 低权重 face-crop 辅助分支
-> 一个最终真实性分数
```

所有新版本必须在 25+25、32+32 和 `ppt_video` 排序开发集上同时通过，才允许替换旧模型或现网页。
## V5 研究方向：真伪检测与生成器排序拆分

更新时间：2026-08-24

### 1. V5 的核心判断

当前任务实际上包含两个不同目标：

```text
目标一：real vs AI 二分类
目标二：真人 > LoRA > Seedance > 多图参考
```

二分类和生成器/管线排序不是同一个监督任务。V4.x 将二分类、表情自然度和四级排序混在一个最终概率中，导致 V3 的二分类指标下降，也没有得到稳定排序。

因此 V5 不再把所有分支直接回传到一个自由 logit，而采用：

```text
V3 冻结真伪专家
        +
独立表情自然度分支
        +
独立生成器归因/排序分支
        ↓
受约束的单调展示分数
```

### 2. V3 级联保护

V3 负责生产决策：

```text
V3 prediction = real / AI
V3 threshold = 使用 V3 validation 选择的阈值
```

排序头不能修改 V3 的二分类决策面。若 V3 判定为真人，展示分数必须处于真人分数带；若 V3 判定为 AI，排序头只能在 AI 内部进行分档。

这是一种工程级回退保护，不代表论文保证；它的作用是防止新表情分支直接把旧 V3 的准确率和召回率冲掉。

### 3. 生成器归因和排序论文

FAME：`FAME: A Lightweight Spatio-Temporal Network for Model Attribution of Face-Swap Deepfakes`。

- 任务是区分不同生成模型/生成管线，而不是只做 real/fake。
- 使用空间和时间特征寻找生成器特有痕迹。
- 说明 LoRA、Seedance、多图参考应被视为不同 source/domain。

参考：

https://arxiv.org/abs/2506.11477

CapST：`CapST: Leveraging Capsule Networks and Temporal Attention for Accurate Model Attribution in Deep-fake Videos`。

- 将模型归因作为多类别任务。
- 使用空间特征、层级表示和时间注意力进行 source attribution。
- 说明“哪一个生成管线更接近真人”不能由普通二分类标签自动推出。

参考：

https://arxiv.org/abs/2311.03782

RankNet、RankSVM：

- 使用成对样本学习 `score(A) > score(B)`。
- 适合表达真人、LoRA、Seedance、多图之间的相对偏好。
- 当前项目的 pairwise logistic 是轻量排序基线，但必须扩充成对数据。

参考：

https://doi.org/10.1145/1102351.1102363

https://doi.org/10.1007/s10791-009-9109-9

### 4. V5 数据要求

当前训练集只有：

```text
实拍 + Seedance
```

这足以训练二分类，但不足以训练：

```text
真人 > LoRA > Seedance > 多图参考
```

V5 需要建立成对或四元组开发数据：

```text
同一身份
同一 Prompt
同一表情脚本
尽量相同机位、背景、光照和时长
真人、LoRA、Seedance、多图参考四类结果
```

`ppt_video` 当前只有 8 个视频，只能做最终排序验收，不能单独训练深度排序器。

### 5. V5 最终展示分

内部保留：

```text
P_v3_real
P_expression_natural
P_generator_rank
```

对外只展示一个分数。建议使用词典序/分段单调映射，而不是自由加权：

```text
V3 判真人：
    score ∈ [0.75, 1.00]

V3 判 AI：
    由 generator rank 在 [0.00, 0.74] 内分档
```

如果业务要求只展示一个数，这个数仍然可以同时满足：

```text
二分类决策由 V3 保证
AI 内部排序由 ranker 负责
```

### 6. V5 不采用的方案

- 不再用 `70% V3 + 20% 表情 + 10% crop` 作为未经验证的固定保证。
- 不再用 8 个 `ppt_video` 视频直接训练大模型排序头。
- 不再用身份相似度作为真实性主证据。
- 不再用测试集调阈值或 profile。
- 不再继续单纯加深 Transformer。

### 7. V5 验收标准

```text
25+25：
准确率和 AI/实拍双向召回不低于 V3

32+32：
同时报告 AI 召回和实拍召回，不能只报告 AI 召回

ppt_video 扩容开发集：
真人 > LoRA > Seedance > 多图参考

所有条件同时满足，才允许替换 V3 或现网页。
```
## 2026-08-24 补充：表情动力学不能直接推出四档真实性序

### 1. 理论可行，但当前任务不是同一个量

特定人物的表情动力学理论上可以帮助判断真伪和生成质量，前提是同时具备：

```text
王兴本人稳定的 onset-apex-offset 表情轨迹
AU 之间的真实耦合关系
眼睛、嘴部、眉毛和下颌的同步关系
不同生成管线在这些轨迹上的稳定差异
成对或四元组排序标签
```

但当前系统量到的主要是：

```text
表情画像是否像王兴
AU 是否落在王兴支持域
表情事件是否存在
landmark/Blendshape 是否连续
```

领导要求的是：

```text
真人 > LTX2.3+自有 LoRA > Seedance2.0 > LTX2.3+多图参考
```

前者是表情兼容性/自然度，后者是生成管线质量和真实性归因，两者不能默认等价。

### 2. 当前表情分失效的直接证据

项目人工分档记录显示，部分 AI 视频的表情证据均值并不低于实拍，约为：

```text
AI 表情证据均值约 71.8
实拍表情证据均值约 66.3
```

这说明当前“表情画像贴合/AU 支持域”更像是在判断：

```text
像不像王兴常见的表情
```

而不是判断：

```text
这套肌肉时序是否来自真实拍摄中的自然驱动
```

LoRA 和多图参考本身就在尽量贴近王兴身份和表情，因此 AI 的表情兼容分可能很高，甚至高于真人。

### 3. 当前主要困难

1. 训练标签只有 `real vs Seedance`，没有 LoRA/多图参考排序标签。
2. `ppt_video` 只有 8 个视频，只能做排序验收，不能支撑深度排序头训练。
3. 当前表情特征仍以采样、统计和画像距离为主，不是完整的阶段对齐轨迹。
4. 不同视频的 Prompt、表情脚本、时长、FPS、机位和光照不完全匹配。
5. LoRA、多图参考和 Seedance 的差异包含身份锁定、融合边界、纹理和管线痕迹，不只包含表情。
6. V4.x 删除 V3 的全帧监督后，纯表情特征的信息量不足，分类指标反而下降。

### 4. 当前技术结论

```text
可以：用王兴表情动力学作为相对自然度辅助分
不可以：用当前表情分直接等价为四档真实性分
```

如果继续坚持表情主路径，必须改成：

```text
同身份、同 Prompt、同动作脚本的成对/四元组数据
轨迹和阶段特征，而不是画像距离
显式 RankNet/RankSVM/归因标签
V3 真伪决策与排序头分离
```

在排序数据不足前，V3 应继续作为真实性基础分支，表情只作为受约束辅助证据。

### V5 完整落实主规格

论文到工程的落地动作、PT/网页双方案、词典序与验收见：

```text
docs/王兴V5落实方案.md
```

该文档为 V5 实现唯一主规格；本文仅保留论文索引与研究判断。
