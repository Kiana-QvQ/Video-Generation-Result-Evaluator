# 2026年5月1日至8月13日详细周工作记录

> 记录范围：`2026-05-01` 至 `2026-08-13`
>
> 按自然周整理，每周安排 2 至 3 次具体工作记录。
>
> 早期没有完整逐日日志的部分，依据文件时间、Git 提交、日志、manifest 和结果目录回填，并标注证据等级；不能确认的内容不写成确定性结论。

## 一、证据等级

- `A：直接证据`：文件、日志、Git 提交或结果目录能直接对应到日期。
- `B：阶段证据`：多个产物能够确认该阶段完成了某类工作，但不能精确到当天。
- `C：待确认`：只有月度材料或项目连续性支持，需要用日历、聊天记录或个人工作日志补充。

每条记录按“完成事项、技术分析、产出指标、状态和下一步”整理。

## 二、按周记录

## 第1周：2026-05-01至2026-05-03

### 记录1｜2026-05-01｜核心工作范围初始化

- 完成事项：启动 5 月核心工作范围整理，将光影、光源控制、人物面部光照和后续视频评估从论文阅读、开源项目部署和历史工程中分离。
- 技术分析：同一工作区同时存在论文、模型缓存、历史项目和正式实验，后续必须按“任务归属、交付产物、研究积累”分层，不能按目录存在与否统计工作。
- 产出指标：明确核心统计范围为 `2026-05-01` 至 `2026-08-13`。
- 状态/下一步：范围初始化完成；下一步盘点光影数据、训练配置和现有模型入口。
- 证据级别：`C`，日期需人工确认。

### 记录2｜2026-05-03｜光影技术路线盘点

- 完成事项：盘点 `DiffusionRenderer`、`IC-Light`、`FaceLight`、`IdentityRelightCommon` 和后续 `LightCtrl` 的工程职责。
- 技术分析：全局重打光、面部局部修复和视频时序控制是不同问题，不能用一个图像级模型替代全部链路。
- 产出指标：确认后续需要同时保留全局光照、人物 mask、face patch、light vector 和视频轨迹字段。
- 状态/下一步：技术路线完成初步拆分；下一步核验本地环境和训练数据。
- 证据级别：`C`。

## 第2周：2026-05-04至2026-05-10

### 记录1｜2026-05-05｜重打光工程环境准备

- 完成事项：准备 IC-Light、DiffusionRenderer 和身份重打光相关目录、依赖入口和模型加载路径。
- 技术分析：IC-Light 适合图像级照明编辑，DiffusionRenderer 涉及 G-buffer/环境光条件，二者输入契约不同，需要分别记录。
- 产出指标：确认 `光影\DiffusionRenderer`、`光影\IC-Light`、`光影\IdentityRelightCommon` 目录。
- 状态/下一步：工程入口完成初步整理；下一步设计 FaceLight 的共享专家接口。
- 证据级别：`B`。

### 记录2｜2026-05-08｜FaceLight 数据结构设计

- 完成事项：确定 FaceLight 的 source/target/light.json 数据结构，以及预处理、训练、验证、推理四段流程。
- 技术分析：矩形或椭圆 mask 容易将背景带入面部光照学习，后续应优先使用人物 alpha/mask，并在贴回时羽化边界。
- 产出指标：形成 `data/raw/{train,val,test}`、`data/processed/face_light`、`data/manifests` 和 `outputs/face_light` 目录约定。
- 状态/下一步：数据契约明确；下一步组织训练样本并执行首轮验证。
- 证据级别：`B`。

## 第3周：2026-05-11至2026-05-17

### 记录1｜2026-05-12｜光照控制变量拆分

- 完成事项：对比图像重打光、神经渲染和人物局部重打光路线，明确全局模型、人脸专家和视频模型的职责边界。
- 技术分析：自然语言 Prompt 只能粗略表达方向，不能保证每帧光源位置稳定，因此需要结构化记录方位角、纬度、强度和颜色。
- 产出指标：确定 `azimuth`、`latitude`、`intensity`、`color` 和轨迹类型为后续控制字段。
- 状态/下一步：光照变量拆分完成；下一步检查 IC-Light 本地推理和 FaceLight 接口。
- 证据级别：`B`。

### 记录2｜2026-05-14｜IC-Light 本地路径检查

- 完成事项：检查 IC-Light 原生 SD1.5 relighting 路径，保留 FLUX/Qwen 作为可选编辑骨干入口。
- 技术分析：UI 中存在模型入口不等于模型已完成本地推理；当前能够确认的是原生 IC-Light 路径。
- 产出指标：`光影\IC-Light\README.md` 记录本地可运行路径、模型加载和缺失依赖时的提示逻辑。
- 状态/下一步：原生图像重打光路径可用；新编辑骨干继续作为后续集成方向。
- 证据级别：`A`。

### 记录3｜2026-05-16｜FaceLight 共享专家架构

- 完成事项：将 FaceLight 设计为共享的人脸真实光照专家，由不同全局方案调用同一个 `FaceLightAdapter`。
- 技术分析：各方案独立训练会造成 mask、权重和评估标准分叉；共享 checkpoint 更适合横向比较。
- 产出指标：确认 `train_face_light.py`、`run_inference.py` 和 `IdentityRelightCommon\identity_relight_common\face_light.py` 的职责。
- 状态/下一步：共享专家接口确定；下一步进入训练数据组织。
- 证据级别：`B`。

## 第4周：2026-05-18至2026-05-24

### 记录1｜2026-05-19｜AI Toolkit 训练环境启动

- 完成事项：启动 AI Toolkit LoRA 训练环境，检查 CUDA、SD1.5、SQLite 任务记录和采样目录。
- 技术分析：日志出现 Triton 缺失警告，但任务仍能进入 pipeline loading；因此将其记录为性能风险，而不是直接判定训练失败。
- 产出指标：生成任务 ID `dddb8647-0455-44fe-a56e-8dbe3be77e62`，建立 `output\test`。
- 状态/下一步：训练任务已启动；下一步核对配置、采样和 loss 数据。
- 证据级别：`A`。

### 记录2｜2026-05-20｜LoRA 训练参数和采样链路验证

- 完成事项：验证 LoRA 网络、优化器、精度、采样和保存策略。
- 技术分析：这是 SD1.5 图像 LoRA 流程验证，不等同 LTX2.3 视频 LoRA；配置中 `save_merged_lora=false`，不能写成最终合并权重交付。
- 产出指标：`3000` steps、CUDA、BF16、AdamW8bit、gradient checkpointing、学习率 `1e-4`、LoRA linear `32`、conv `16`、采样图 `10` 张、seed `42`。
- 状态/下一步：训练流程验证完成；当前目录未确认最终 LoRA 权重。
- 证据级别：`A`。

### 记录3｜2026-05-22｜训练结果边界确认

- 完成事项：检查 `config.yaml`、`.job_config.json`、`log.txt`、`logs\0_log.txt`、`loss_log.db` 和 `samples`。
- 技术分析：日志和采样只能证明流程跑通，不能证明最终模型质量；交付结论还需要最终权重、推理配置和可复现实例。
- 产出指标：保留完整训练配置、日志、数据库和 `10` 张采样图。
- 状态/下一步：AI Toolkit 流程验证完成；下一步转向人像光照数据和 FaceLight 训练。
- 证据级别：`A`。

## 第5周：2026-05-25至2026-05-31

### 记录1｜2026-05-25｜FaceLight 首轮验证

- 完成事项：执行 FaceLight 训练/验证流程，检查 source、target、人物 mask 和 light vector 是否能形成完整样本。
- 技术分析：首轮验证样本少，只能用于过程诊断，不能作为泛化结论；需重点观察 mask、背景泄漏和高光区域误差。
- 产出指标：5 个验证样本 mean L1 `0.3439`、mean PSNR `9.6154`。
- 状态/下一步：训练链路可运行；下一步重建 mask 并扩大验证集。
- 证据级别：`A`。

### 记录2｜2026-05-27｜人物 mask 和光照素材整理

- 完成事项：整理人像原图、目标打光图和 light JSON，检查人物 alpha 与目标图空间对齐。
- 技术分析：矩形 mask 会让模型学习背景边界伪影，后续预处理需要优先采用 RMBG/人物分割，椭圆仅作为兜底。
- 产出指标：形成 FaceLight raw/processed/manifests 数据链路，保留人物 mask、训练 preview 和推理缓存。
- 状态/下一步：数据组织完成；下一步重建数据并比较 mask 修改前后效果。
- 证据级别：`B`。

### 记录3｜2026-05-29｜全局模型与面部专家接口分离

- 完成事项：明确全局方案负责整体光照和背景，FaceLight 负责局部面部高频细节及贴回。
- 技术分析：两者必须共享同一 light vector，否则会出现脸部和身体光照方向不一致；推理接口还需支持 `face_bbox`、`face_mask`、`hf_strength` 和 debug mask。
- 产出指标：`FaceLight\README.md` 固化训练、测试和推理参数。
- 状态/下一步：接口边界完成；下一步进入光照控制数据准备。
- 证据级别：`A`。

## 第6周：2026-06-01至2026-06-07

### 记录1｜2026-06-01｜表情视频数据集首轮生成

- 完成事项：按固定起始帧和固定窗口切分表情源视频，生成自动描述、视频报告和训练清单。
- 技术分析：统一片段长度和采样策略，才能比较不同表情和不同源视频；清单保留源文件、起止帧、fps、分辨率和片段文件。
- 产出指标：`19` 个源文件夹、`627` 个源视频、每个源视频 `5` 段、每段 `49` 帧、总计 `3135` 个片段。
- 状态/下一步：首轮数据集结构完成；下一步复核 caption 和标签建议。
- 证据级别：`A`。

### 记录2｜2026-06-03｜表情标签字段复核

- 完成事项：复核表情类别、嘴部状态、眉毛/眼睛状态、视角、俯仰角和光照描述。
- 技术分析：仅记录“开心/悲伤”等粗标签不足以支持动作评估，需要保留 `Blinking`、`Crying`、嘴部开合、眉毛紧张和视角等细节。
- 产出指标：`dataset_auto.csv`、`dataset_auto.json`、`video_report.csv` 与 `label_suggestions` 建立对应关系。
- 状态/下一步：标签字段基本稳定；下一步处理路径、缺失和重复问题。
- 证据级别：`B`。

### 记录3｜2026-06-06｜标签压缩包和下游输入准备

- 完成事项：整理 `lable`、`label_suggestions`、视频目录和压缩包，为后续 LTX2.3 和表情专项提供统一输入。
- 技术分析：压缩包用于迁移，训练仍以 CSV/JSON 清单为准，不能在解压后从目录名重新推断标签。
- 产出指标：`视频标签.zip` 约 `92 MB`；标签目录和标签建议目录各约 `115` 个。
- 状态/下一步：标签基础包形成；下一步继续修正描述并统一片段数据。
- 证据级别：`B`。

## 第7周：2026-06-08至2026-06-14

### 记录1｜2026-06-08｜自动描述和路径修正

- 完成事项：更新 `dataset_auto.json`，补充面部细节和动作状态描述，修正跨 Windows/Linux/云端环境的路径表示。
- 技术分析：训练清单中的绝对路径不能直接跨机器使用，必须通过根目录替换或 manifest 转换迁移。
- 产出指标：`dataset_auto.json` 在 `2026-06-08` 有更新，继续保留 caption、media path 和源片段关系。
- 状态/下一步：清单可迁移性提升；下一步继续扩大光照控制数据。
- 证据级别：`A`。

### 记录2｜2026-06-10｜固定切片协议复核

- 完成事项：复核 `49` 帧切片、`40 FPS` 源视频、分辨率和多个时间窗口。
- 技术分析：只截取前段会丢失后段表情变化；当前协议使用 `0/164/328/493/657` 等窗口覆盖头、中、尾。
- 产出指标：`video_report.csv` 保留 `width`、`height`、`fps`、`frames`、`start_frame`、`end_frame`。
- 状态/下一步：切片协议复核完成；下一步转入结构化光照控制。
- 证据级别：`B`。

## 第8周：2026-06-15至2026-06-21

### 记录1｜2026-06-15｜面部细节和光照数据补充

- 完成事项：继续整理面部细节、光照条件、身份参考图和目标打光图。
- 技术分析：身份特征、面部高频纹理和光照方向是不同变量，需要分别保存，不能只保留一张合成结果图。
- 产出指标：FaceLight manifest、processed 样本、人物 mask 和推理缓存继续完善。
- 状态/下一步：数据链路保持可复现；具体训练批次需人工补充。
- 证据级别：`C`。

### 记录2｜2026-06-18｜光照轨迹字段设计

- 完成事项：整理单白光控制所需的路径、方位角、纬度、速度、强度、颜色、帧数和 fps 字段。
- 技术分析：Prompt 不能保证每帧光源位置稳定，必须使用 sidecar JSON/manifest 记录确定性轨迹。
- 产出指标：形成 `left_to_right`、`top_to_bottom`、`orbit` 等路径类型。
- 状态/下一步：结构化光照协议完成；下一步生成 LTX2.3 stage0 数据包。
- 证据级别：`B`。

## 第9周：2026-06-22至2026-06-28

### 记录1｜2026-06-22｜光影主目录和共享模块整理

- 完成事项：整理 `FaceLight`、`IC-Light`、`IdentityRelightCommon`、`LightCtrl`、`LTX2.3` 和数据目录。
- 技术分析：不同方案重复保存训练脚本和模型会造成权重漂移，因此将通用人脸光照逻辑和共享 checkpoint 独立。
- 产出指标：形成共享 FaceLight 的目录约定和 README 流程。
- 状态/下一步：共享代码边界明确；下一步整理数据清单和上传包。
- 证据级别：`A`。

### 记录2｜2026-06-26｜LTX2.3 云端训练迁移方案

- 完成事项：准备 LTX2.3 的 Photo、光照 manifest、caption 和训练配置，规划 AutoDL/AI Toolkit 路径迁移。
- 技术分析：LTX2.3 22B 在本地 3090 24GB 上存在显存、TDR 和黑屏风险，因此需要 low-vram、云端路径和 `--resume` 续跑方案。
- 产出指标：README 固化 `576x576x25`、low-vram、Gemma 分片检查和训练续跑流程。
- 状态/下一步：训练输入和迁移方案完成；下一步补齐 stage0 单白光数据。
- 证据级别：`B`。

## 第10周：2026-06-29至2026-07-05

### 记录1｜2026-06-30｜视频标签包阶段封装

- 完成事项：完成视频标签目录、标签建议目录和压缩包封装，保持原视频、标签和建议文件映射。
- 技术分析：压缩包只是迁移介质，训练审计仍以 `dataset_auto.csv/json` 和 `video_report.csv` 为准。
- 产出指标：`视频标签.zip` `92,125,512` bytes；标签目录和建议目录各约 `115` 个。
- 状态/下一步：标签阶段封装完成；下一步统一 LTX2.3 训练输入。
- 证据级别：`A`。

### 记录2｜2026-07-02｜LTX2.3 对照协议建立

- 完成事项：建立基础模型/LoRA 对照配置，固定控制图、Prompt、seed、帧数、fps、分辨率、推理步数和 guidance。
- 技术分析：只有在变量固定时，视频差异才能归因于 LoRA 或数据组合；SUMMARY 明确写入相同条件。
- 产出指标：形成 `LTX2.3_RGB_slow3.0`、`LTX2.3_V2_wx_mixed`、`LTX2.3_V2_wx_only_long` 三组配置。
- 状态/下一步：对照协议完成；下一步生成视频并进行审查。
- 证据级别：`A`。

### 记录3｜2026-07-05｜实验路径和云端输出复核

- 完成事项：复核 caption、控制图、LoRA 权重、配置和 `/root/outputs` 输出路径的对应关系。
- 技术分析：云端绝对路径不能直接作为本地复现路径，记录中必须同时保留实验名、配置名和输出文件名。
- 产出指标：三组对照目录均有 `SUMMARY.txt`、base/lora 子目录和 README。
- 状态/下一步：实验元数据完整性提升；下一步进入 LightCtrl 轨迹验证。
- 证据级别：`B`。

## 第11周：2026-07-06至2026-07-12

### 记录1｜2026-07-07｜单白光控制 manifest 扩展

- 完成事项：扩展 LTX2.3 单白光控制数据，统一轨迹类型、方位角、纬度、强度、颜色、帧数和 fps。
- 技术分析：同一轨迹需要覆盖不同人物和不同纬度，避免模型把光照方向与人物姿态绑定。
- 产出指标：处理样本统一为 `97` 帧、`25 FPS`，白光强度示例为 `0.7`。
- 状态/下一步：manifest 可用于训练/上传；下一步建立 stage0 筛选包。
- 证据级别：`B`。

### 记录2｜2026-07-10｜LTX2.3 与 LightCtrl 字段对齐

- 完成事项：将 LightCtrl 显式光照控制字段与 LTX2.3 数据侧字段对齐。
- 技术分析：`bg_source` 适合 demo，但不适合精确轨迹；显式 `light_control` 应优先于旧预设。
- 产出指标：统一 `path`、`azimuth_start/end`、`latitude_start/end`、`speed`、`intensity`、`color` 和 `softness`。
- 状态/下一步：控制接口对齐；下一步执行 preview 和视频重打光。
- 证据级别：`B`。

## 第12周：2026-07-13至2026-07-19

### 记录1｜2026-07-13｜LightCtrl 光照 preview

- 完成事项：使用显式光照控制脚本生成不加载扩散模型的光照条件 preview。
- 技术分析：先验证光照 map 可以排除扩散模型生成因素，避免错误轨迹消耗 GPU。
- 产出指标：生成 `light_control_preview.mp4`，检查左到右、上到下、环绕和 Blackman 路线。
- 状态/下一步：光照条件可视化完成；下一步进行视频重打光。
- 证据级别：`A`。

### 记录2｜2026-07-14｜LightCtrl 逐帧路线审查

- 完成事项：抽取输入、控制图、过程帧和输出帧，对路线进行逐帧检查。
- 技术分析：视频整体连续不代表局部稳定，需检查光斑跳变、面部高光漂移和背景不一致。
- 产出指标：生成 `stage0_l2r`、`stage0_t2b`、`stage0_orbit`、`blackman` 等过程图，审查图片总数 `36` 张。
- 状态/下一步：路线级质量检查完成；下一步保存最终重打光视频和 stage0 包。
- 证据级别：`A`。

### 记录3｜2026-07-15｜重打光视频和 stage0 包

- 完成事项：完成指定人物视频重打光，并整理可上传的单白光 stage0 数据。
- 技术分析：stage0 明确排除 legacy POLAR、WX auxiliary、RGB 和 head-motion clips，防止不同协议混入同一训练阶段。
- 产出指标：生成 `relight_music_girl.mp4`；stage0 `index.json` 记录 `96` 个样本。
- 状态/下一步：LightCtrl preview、重打光和审查完成；LTX2.3 stage0 包可迁移到 `/root/ai-toolkit`。
- 证据级别：`A`。

## 第13周：2026-07-20至2026-07-26

### 记录1｜2026-07-20｜LTX2.3 基础/LoRA 对照整理

- 完成事项：整理三组基础模型与 LoRA 对照视频，核对控制图、seed、视频长度、fps 和输出文件。
- 技术分析：评估不只看单帧亮度，还要比较光照控制、身份稳定、细节和时序连续性。
- 产出指标：三组典型条件为 `161@24`、`97@25`、`156@24`，均为 `768x1024`、seed `42`、sample steps `30`、guidance `4.0`。
- 状态/下一步：形成 `7` 个 MP4 和 `3` 个汇总文件；下一步将代表性视频送入评估器。
- 证据级别：`B`。

### 记录2｜2026-07-23｜评估器项目骨架

- 完成事项：建立评估器根目录、模型配置、Web 入口、评估模块、数据目录、脚本和测试目录。
- 技术分析：评估器需要同时处理无参考质量、参考图身份、GT 像素指标、Prompt 语义和时序稳定，因此先拆分输入契约和 category 状态。
- 产出指标：形成 `evaluator`、`web`、`outputs`、`config`、`tests` 和 `scripts`。
- 状态/下一步：项目骨架完成；下一步实现 GPU 调度和 Web 任务接口。
- 证据级别：`B`，项目 Git 初始化可确认于 `2026-07-26`。

### 记录3｜2026-07-26｜首批 Web 评估任务

- 完成事项：运行首批 Web 评估，验证上传、任务状态、结果 JSON、summary CSV、frame metrics CSV 和结果 MP4。
- 技术分析：GPU 任务需要队列、任务目录和 worker claim，避免同一任务被多进程重复消费。
- 产出指标：当天 `3` 个任务、`3` 份 result JSON、`3` 份 summary CSV、`3` 份 frame metrics CSV、`3` 个 MP4。
- 状态/下一步：异步评估链路可用；下一步增加 gRPC 和多条件参考输入。
- 证据级别：`A`。

## 第14周：2026-07-27至2026-08-02

### 记录1｜2026-07-27｜Web、GPU 调度和 gRPC

- 完成事项：完善五维评估、Web 页面、自动 GPU 调度和 gRPC 备用接口。
- 技术分析：HTTP 与 gRPC 必须共享任务队列、结果目录、安全限制和 JSON 契约，gRPC 只替换传输层。
- 产出指标：新增 `GRPC_API.md`、`PUBLIC_DEPLOYMENT.md`、`frame_audit.proto`、`grpc_server.py` 和传输测试。
- 状态/下一步：双入口结构完成；下一步进行参考图、GT 视频和 Prompt 对照。
- 证据级别：`A`。

### 记录2｜2026-07-28｜Prompt 对照和表情数据集落地

- 完成事项：完成 5 组匹配/冲突 Prompt 对照评估，同时落地 `all_expression` 数据集。
- 技术分析：无参考图时身份分类必须为 unavailable，不能填入伪造分数；完整参考和 partial 参考需要保留不同 coverage。
- 产出指标：完整案例综合分 `76.075`、身份 `0.9435`、时间稳定 `0.9089`；表情数据集 `627` 个源视频、`3135` 个片段。
- 状态/下一步：评估器输入契约和数据集输入可运行；下一步批量 Web 运行和 AU 数据提取。
- 证据级别：`A`。

### 记录3｜2026-07-31｜批量评估和 AU 数据积累

- 完成事项：批量运行评估，持续生成 result JSON、summary CSV、frame metrics CSV 和 MP4；同步整理 AU/Face Mesh 数据。
- 技术分析：不同任务参考条件覆盖度不同，必须保存 `complete/partial` 和 category unavailable 状态，不能直接横向排名。
- 产出指标：当天新增 `59` 个任务、`59` 份 result JSON、`118` 份 CSV 和 `59` 个 MP4。
- 状态/下一步：批量运行稳定；下一步把 AU 数据从普通表情分数中拆出 Wang Xing 专项。
- 证据级别：`A`。

## 第15周：2026-08-03至2026-08-09

### 记录1｜2026-08-03｜批量任务状态和结果完整性

- 完成事项：继续批量运行 Web 评估，补充结果状态、失败/部分结果处理和下载路径。
- 技术分析：目录中有 MP4 不代表任务完整，必须同时检查 result JSON、summary CSV、frame metrics CSV 和 status。
- 产出指标：当天 `6` 个任务、`5` 份 result JSON、`10` 份 CSV 和 `10` 个 MP4。
- 状态/下一步：结果状态统计逻辑稳定；下一步进行 AU 专项和部署安全整理。
- 证据级别：`A`。

### 记录2｜2026-08-06｜AU 专项和部署边界

- 完成事项：完成 AU 数据验证、正负样本边界检查，并补充上传限制、队列单所有者和依赖环境隔离说明。
- 技术分析：普通五维 expression score 与 Wang Xing AU/身份专项含义不同；VBench、VLM、LibreFace 不能强行装进同一环境。
- 产出指标：真实 AU 样本 `645`、负样本 `48`、六类表情；六类分类准确率 `22.33%`，不作为最终结论。
- 状态/下一步：AU 专项边界明确；下一步进行 Wang Xing 留出验证和取证 profile。
- 证据级别：`A`。

### 记录3｜2026-08-07｜Wang Xing 专项和初版取证

- 完成事项：建立 Wang Xing 身份/表情专项验证，并构建真人与 Seedance 的面部运动、纹理和来源 profile。
- 技术分析：身份、表情支持和来源判断是三个不同任务；普通六类分类留出准确率约 `25%`，因此改用 support-domain compatibility。
- 产出指标：表情训练 `441`、测试 `120`；兼容度均值 `0.8149`、兼容率 `0.825`；身份诊断 ROC-AUC `1.0`；纹理小样本 ROC-AUC `0.889`。
- 状态/下一步：专项初版完成；下一步比较硬阈值和学习融合。
- 证据级别：`A`。

## 第16周：2026-08-10至2026-08-13

### 记录1｜2026-08-10｜取证融合和留出验证

- 完成事项：对 Wang Xing source profile、面部运动分支和纹理分支进行硬阈值与学习融合比较，按 source video/generation batch 做留出。
- 技术分析：硬阈值 `0.50/0.55` 准确率只有 `63%/66%`，说明固定规则不足；无泄漏逻辑回归融合提升明显，但仍只代表当前留出集。
- 产出指标：硬融合阈值 `0.50` 准确率 `63%`，阈值 `0.55` 准确率 `66%`，无泄漏融合 `100` 条留出数据准确率 `96%`。
- 状态/下一步：可作为阶段性实验结论；下一步扩大跨批次、跨模型验证并检查校准误差。
- 证据级别：`A`。

### 记录1b｜2026-08-12至2026-08-13｜学习融合、视频.pt与汇报口径

- 完成事项：完成 source 无泄漏重建、AU 学习头复测、双尺度视频 `.pt`（24f@1k+8f@2k）训练测试；统一对领导汇报口径。
- 技术分析：固定加权不足；学习融合与视频旁路在同留出上峰值很高，但对外应报保守值并写清域风险。
- 产出指标：内部峰值 AU 约 `96%/96%`、视频 `.pt` 约 `96%/98%`；**对外口径约 `92%`（相对前期约 `78%`）**；内部口径文稿仅本地保存，公开说明见 `docs/仓库内容与数据策略.md`。
- 状态/下一步：汇报材料就绪；下一步跨引擎复测与 AU+视频后融合（先项目侧验证再进 `evaluator/modules`）。
- 证据级别：`A`。

### 记录2｜2026-08-11｜人工复核原始数据规范化

- 完成事项：规范化 `experiments_20260811` 原始实验数据，统一 prompt、参考图、参考视频、候选视频和模型字段。
- 技术分析：同一模型的两个候选不能自动当作模型比较题；无法证明跨模型条件一致的候选必须放入跳过清单或人工复核。
- 产出指标：生成 `rename_map.json`、任务元数据和 `performance_v8` 构建输入；记录输出不足、同模型不可比较和跨模型需人工复核原因。
- 状态/下一步：原始素材规范化完成；下一步生成版本化数据集和 SQLite 索引。
- 证据级别：`A`。

### 记录3｜2026-08-12至2026-08-13｜performance_v8 和交付归档

- 完成事项：生成 `performance_v8`，完善 reviewer identity、重复投票限制、任务配对、跳过批次和部署安全文档，并整理 5 月以来详细工作记录。
- 技术分析：`ai_real_anchor`、`model_comparison` 和 `control` 必须分开；control 不进入模型排名，`needs_manual_review` 不自动分发。
- 产出指标：总任务 `67`，其中可投票内容题 `58`、control task `8`、manual review `1`；资产 `146` 个，跳过批次 `25` 个，含 `1` 道 LTX2.3 与 Seedance 2.0 对比题。
- 状态/下一步：人工复核数据集和服务说明完成；后续补充实际投票统计和跨批次取证验证。
- 证据级别：`A`。

## 三、项目阶段结论

### 1. 光影和 LTX2.3

- 已完成训练环境验证、FaceLight 共享专家路线、单白光控制 manifest、stage0 上传包、LightCtrl preview、视频重打光和 LTX2.3 基础/LoRA 对照。
- 已确认：stage0 `96` 个样本；典型样本 `97` 帧、`25 FPS`、强度 `0.7`；LightCtrl `1` 个 preview、`1` 个重打光视频、`36` 张审查图；LTX2.3 三组对照输出。
- 边界：AI Toolkit 5 月试验确认训练流程，但当前目录未确认最终 LoRA 权重；FaceLight 指标属于工程诊断，不代表生产泛化。

### 2. 视频标签和表情数据

- 已完成源视频整理、固定窗口切片、自动描述、标签建议、视频报告、压缩包和 `all_expression` 数据集。
- 已确认：`627` 个源视频、`3135` 个片段、每段 `49` 帧、`19` 个源文件夹。
- 边界：自动 caption 是训练输入，不等同人工情绪真值；后续 AU 专项采用 recording group holdout 和 support-domain compatibility。

### 3. 视频生成结果评估器

- 已完成五维评分、参考图/GT/参考动作输入、Prompt 对照、Web 任务、GPU 队列、gRPC 备用接口、部署和安全限制。
- 已确认：`2026-07-26` 至 `2026-08-12` 产生 `407` 个任务目录，`400` 份可解析 result JSON。
- 边界：不同任务 weight coverage 不同；`partial` 结果不能与 `complete` 结果直接排总榜。

### 4. Wang Xing、取证和人工复核

- 已完成 AU/身份专项、表情支持域、面部运动 profile、纹理 profile、硬阈值、逻辑回归融合、校准器、视频双尺度 `.pt`、人工复核数据集和服务。
- 已确认：Wang Xing 表情支持兼容度均值约 `0.815`；取证纹理小样本 ROC-AUC `0.889`；内部无泄漏融合约 `96%`、视频 `.pt` 约 `98%`；**对领导口径约 `92%`（相对前期约 `78%`）**；人工复核 `58` 个可投票任务。
- 边界：峰值只代表当前 `100` 条同协议留出；取证仍需跨生成批次、跨模型和更大样本验证；六类表情分类不作为最终结论。内部汇报口径仅本地保存；公开策略见 `docs/仓库内容与数据策略.md`。

## 四、需要人工补充或确认

- `2026-05-01` 至 `2026-05-17` 的具体工作日，需要用个人日历、聊天记录或工作日志补齐。
- `2026-05-25` 至 `2026-06-28` 的 FaceLight、光影数据和方案调整，目前只能由阶段产物证明。
- LTX2.3 三组对照的部分运行日期由输出目录和 SUMMARY 回填，严格日级证明需要云端任务日志。
- 评估器 `407` 个任务需要继续标记正式测试、开发调试和重复任务。
- `performance_v8` 已完成任务构建，但实际投票数量、有效 reviewer 数量和模型偏好统计尚未写入。

## 五、可直接复制的周报格式

```text
日期：
项目：
完成事项：
技术分析/处理的问题：
产出文件/结果：
数量或指标：
当前状态：
下一步：
证据级别：
```

## 六、记录边界

- 论文阅读、RenderFormer、FramePack、ComfyUI、UniRelight、IC-Light 等没有明确正式任务归属的内容，不进入本周记录。
- 当前记录中的 IC-Light、Diffusion Renderer 和论文只作为光影方案参考；LightCtrl 和 LTX2.3 的实际视频、配置和审查图才是核心项目证据。
- 论文作者指标、开源项目 README 指标和本地实验指标不得混写。
